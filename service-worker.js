// service-worker.js
// A minimal service worker file to satisfy PWA requirements.
// Full caching logic would go here, but is omitted for simplicity.
self.addEventListener('fetch', function(event) {
  // Let the browser handle the request
  return;
});
